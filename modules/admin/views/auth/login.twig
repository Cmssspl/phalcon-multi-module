<?php
use \Phalcon\Tag;
use \Phalcon\Mvc\Url;
?>

<h1>Logowanie</h1>

{#<?php print_r($test); ?>#}

<div id="messages">{{ flash.output() }}</div>

{{ form( null, 'method': 'post') }}
	Login: {{ loginForm.render('nick') }}
	Haslo: {{ loginForm.render('pass') }}
	
	{#{{ loginForm.render('save') }}#}
	{{ submit_button('Login') }}
{{ endForm() }}